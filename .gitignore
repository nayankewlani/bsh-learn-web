# Environment variables — NEVER commit these
.env
.env.*
!.env.example

# Sub-project git repos (managed separately)
API-server/
mobile/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build archives — never commit these, they bloat the repo and fill the server disk
*.tar.gz
*.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
